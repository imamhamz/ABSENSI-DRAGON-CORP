import {supabase,EMPLOYEE_TABLE,ATTENDANCE_TABLE} from "./supabase.js";const map=L.map("map").setView([-6.2,106.8],10);L.tileLayer("https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png",{attribution:"© OpenStreetMap"}).addTo(map);let q=await supabase.from(ATTENDANCE_TABLE).select("employee_id,tanggal,jam_masuk,latitude,longitude,accuracy").not("latitude","is",null);let e=await supabase.from(EMPLOYEE_TABLE).select("id,nama");let m=new Map((e.data||[]).map(x=>[x.id,x]));let b=[];(q.data||[]).forEach(r=>{let p=[Number(r.latitude),Number(r.longitude)];b.push(p);L.marker(p).addTo(map).bindPopup(`<b>${m.get(r.employee_id)?.nama||"Pegawai"}</b><br>${r.tanggal} ${r.jam_masuk||""}`)});if(b.length)map.fitBounds(b);